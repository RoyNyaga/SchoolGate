<!DOCTYPE html>
<html>
  <head>
    <title>SchoolGate Technologies.</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="description" content="SchoolGate: Manage your school with a click, Take the lead by growing your school with the right Statistics.">
    <link rel="icon" href=<%= image_url("tap-logo.png") %> >
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="true" />
    <link href="https://fonts.googleapis.com/css2?family=Josefin+Sans:ital,wght@0,100;0,300;0,400;0,500;0,600;0,700;1,100;1,300;1,400;1,500;1,600;1,700&family=Noto+Serif+Display:ital,wght@1,200&family=Roboto:wght@400;700&display=swap" rel="stylesheet" />
  </head>
  <body class="background-grey">
    <%= render "shared/navbar" %>
    <div id="main-flash" class="flash">
      <%= render "shared/flash_messages" %>
    </div>
    <div class="overall-wrapper-div" style="margin-top: 86px;">
      <%= yield %>
      <%= render "shared/footer" %>
    </div>
  </body>
</html>
