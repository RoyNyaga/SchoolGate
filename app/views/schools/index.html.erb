<div class="container main-wrapper-padding">
  <h4 class="text-center py-5">Your Schools</h4>
  <%= link_to "New school", new_school_path, class: "general-link" %>
  <div class="row">
    <% @schools.each do |school| %>
      <div class="col-md-4 my-2">
        <%= render school %>
      </div>
    <% end %>
  </div>
</div>
