<%= form_with(model: competence) do |form| %>
  <% if competence.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(competence.errors.count, "error") %> prohibited this competence from being saved:</h2>
      <ul>
        <% competence.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div>
    <%= form.label :school_id, class: "d-none" %>
    <%= form.hidden_field :school_id, value: current_school.id %>
  </div>
  <div>
    <%= form.label :school_class_id, class: "d-none" %>
    <%= form.hidden_field :school_class_id, value: @subject.school_class_id %>
  </div>
  <div>
    <%= form.label :suubject_id, class: "d-none"%>
    <%= form.hidden_field :subject_id, value: @subject.id %>
  </div>
  <div class="d-flex align-items-center">
    <%= form.text_field :title, class: "general-input-styles", placeholder: "Enter competence" %>
    <%= form.submit "save", class: "model-form-btn" %>
  </div>
<% end %>
