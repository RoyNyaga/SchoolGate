<div class="d-flex justify-content-between align-items-end">
  <p><%= topic.title %> - p -</p>
  <p data-action="click->progresses#addTopicToForm"
    data-topic-id="<%= topic.id %>"
    data-topic-title="<%= topic.title %>"
    data-topic-thumpalertid="thump_alert_topic_<%= topic.id %>"
    >Select <span class="thump-up-alert d-none" id="thump_alert_topic_<%= topic.id %>">👍</span></p>
</div>
