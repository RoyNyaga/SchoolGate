<div class="container">
  <h4 class="my-5 text-center">Results</h4>
  <div class="row">
    <% @course_registions.each do |course_registion| %>
      <div class="col-md-3">
        <%= render partial: "course_results/grouped_course_results", locals: { course_registion: course_registion } %>
      </div>
    <% end %>
  </div>
</div>
