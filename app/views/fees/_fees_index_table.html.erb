<table class="table table-hover table-head-styles">
  <thead>
    <tr>
      <th scope="col">St names</th>
      <th scope="col">class / fee</th>
      <th scope="col">Year</th>
      <th scope="col">Status</th>
    </tr>
  </thead>
  <tbody>
    <% fees.each do |fee| %>
      <%= render partial: "fee_index_card", locals: { fee: fee } %>
    <% end %>
  </tbody>
</table>
