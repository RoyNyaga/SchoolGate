 <%= turbo_frame_tag custom_id_generator("update", record: @main_topic) do %>
  <%= render "form", main_topic: @main_topic %>
  <div class="d-flex">
    <%= link_to "Cancel", for_teacher_subject_path(@main_topic.subject) %>
    <%=button_to "Delete Instead", main_topic_path(@main_topic), method: :delete %>
  </div>
<% end %>
